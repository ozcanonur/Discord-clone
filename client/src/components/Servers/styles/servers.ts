import createStyles from '@material-ui/core/styles/createStyles';

const serversStyles = createStyles({
  list: {
    padding: 0,
  },
  listItem: {
    padding: 0,
  },
});

export default serversStyles;
